[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-v"
testpaths = [
    "benchopt/tests",
]

[tool.check-manifest]
ignore = [
  "*.yml",
  ".coveragerc",
  ".circleci/*",
  "continuous_integration/*",
  "Makefile",
  "benchmarks/**",
  "roadmap.md",
]
